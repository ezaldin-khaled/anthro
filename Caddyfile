# Single block: avoids 400 from catch-all or Host header issues.
# Forward Host without port so upstream (nginx) doesn't reject the request.
anthrotech.ae, www.anthrotech.ae {
  reverse_proxy frontend:80 {
    header_up Host {host}
    header_up X-Forwarded-Proto https
  }
}
